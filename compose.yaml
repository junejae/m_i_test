services:
  web:
    image: nginx:1.27-alpine
    container_name: m_i_test_web
    ports:
      - "${WEB_PORT:-8080}:80"
    volumes:
      - ./app:/usr/share/nginx/html:ro
    restart: unless-stopped
